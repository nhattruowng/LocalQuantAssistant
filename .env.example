# LocalQuant Assistant environment template.
# Copy to `.env` for local overrides. Do not commit real secrets.

LOCALQUANT_ENV=local
APP_CONFIG_PATH=src/config/settings.yaml
LOCALQUANT_DB_PATH=data/localquant.db
LOG_LEVEL=INFO
